type Mutation {
	saveFollow(data: SaveFollowInput): Follow!
	updateFollowStatus(data: UpdateFollowStatus): Follow!
}

input UpdateFollowStatus {
	followed: ID!
	follow_status: Int!
}

input SaveFollowInput {
	followed: ID!
}