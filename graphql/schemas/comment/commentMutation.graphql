type Mutation {
	saveComment(data: SaveCommentInput): Comment!
	deleteComment(data: deleteCommentInput): Comment
}

input SaveCommentInput {
	post_id: String!
	comment: String!
}

input deleteCommentInput {
	commentId: ID!
}